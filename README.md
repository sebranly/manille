# manille

Manille Engine

## Development

### Workflow

`nvm` is being used in order to have a common Node version

### Publishing

After the commits are merged to `main`:

```
npm run test
npm version patch
npm publish
```
